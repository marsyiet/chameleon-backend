class AuditActions:

    LOGIN = "LOGIN"

    LOGOUT = "LOGOUT"

    PLATFORM_INITIALIZED = (
        "PLATFORM_INITIALIZED"
    )

    USER_CREATED = (
        "USER_CREATED"
    )

    USER_UPDATED = (
        "USER_UPDATED"
    )

    USER_DELETED = (
        "USER_DELETED"
    )

    ORGANIZATION_CREATED = (
        "ORGANIZATION_CREATED"
    )

    ORGANIZATION_UPDATED = (
        "ORGANIZATION_UPDATED"
    )

    ORGANIZATION_DELETED = (
        "ORGANIZATION_DELETED"
    )